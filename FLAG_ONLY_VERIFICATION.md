# Flag-Only Verification Notes

- `shared/learning.ts`의 클라이언트 문제 데이터는 증거, 문제 설명, 단서, 대응 노트만 포함하며 선택지와 정답 후보를 포함하지 않는다.
- 서버는 `HG{...}` 형식의 플래그를 별도로 검증하고, 일치 여부만 반환한다.
- 대시보드 응답은 이전 레벨 평가 상태를 반환하지 않으며, 해결한 문제와 최종 기록 상태만 사용한다.
- 선택지·정답 후보·레벨 평가 상태 필드에 대한 검색 결과는 스키마의 미사용 레거시 테이블 또는 플래그 검증 결과의 `correct` 상태에만 해당한다.
