# Hack Guidance 워게임 플레이 모델

## 런 구조

각 문제는 `사건 파일 → 타깃 런처 → 서비스 표면 → 분석 도구 → 회수 도크` 순서로 진행한다. 사건 파일은 왜 조사하는지만 전달하고, 타깃 런처는 하나 이상의 독립 서비스 표면을 연다. 분석 도구는 문제 유형에 맞는 관찰만 제공하며, 회수 도크는 Hack Guidance 서버 검증으로만 플래그를 제출한다.

| 플레이 모델 | 적용 유형 | 서비스 표면 | 분석 도구 | 회수 단서 |
|---|---|---|---|---|
| Identity trail | 인증·세션 | 로그인, 복구, 계정 세션 | 쿠키·응답 차이 | 세션/역할 아티팩트 |
| Artifact hunt | 파일·경로 | 자료실, 배포 저장소, 업로드 큐 | 파일 목록·메타데이터 | 문서/경로 아티팩트 |
| Object pivot | IDOR·권한 | 디렉터리, 레코드 상세, 권한 매트릭스 | 객체 맵·요청 전환 | 소유권 아티팩트 |
| Request replay | 입력·API | 검색, 프로필, REST 엔드포인트 | 요청 작성기·응답 뷰어 | 응답 아티팩트 |
| Render trace | 출력·콘텐츠 | 공지, 댓글, 미리보기 | 렌더 차이·DOM 관찰 | 출력 아티팩트 |
| Incident review | 종합·방어 | 사건 보드, 감사 기록, 보고서 | 사건 타임라인·증거 사슬 | 결론 아티팩트 |

## 50개 매핑 원칙

1~10은 Identity trail과 Artifact hunt를 주로 사용한다. 11~20은 Request replay와 Artifact hunt를 사용한다. 21~30은 Render trace와 Request replay를 사용한다. 31~40은 Object pivot과 Incident review를 사용한다. 41~50은 앞선 표면 둘 이상을 묶는 Incident review를 사용한다. 각 노드는 같은 유형이더라도 서비스 창 수, 기본 열린 창, 관찰 도구, 회수 결과 표시 위치를 달리한다.

## 디자인 원칙

메인 Hack Guidance는 어두운 네트워크 보드로 유지한다. 문제 런처는 완전히 다른 운영체제형 작업 공간으로 전환한다. 각 서비스 표면은 실제 웹사이트처럼 보이며, 서비스 간 이동은 탭·사이드 도크·팝오버를 통해 이루어진다. Hack Guidance 브랜드와 플래그 제출 도크는 작업 공간의 보조 오버레이로만 존재한다.
