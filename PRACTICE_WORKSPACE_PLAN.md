# 브라우저 내 실습 워크스페이스 적용 계획

## 진단 결과

기존 50개 노드는 증거를 읽고 플래그를 제출하는 흐름으로 통일되어 있었습니다. 따라서 사용자는 요청 값, 출력 문맥, 권한 경계 같은 보안 개념을 **직접 바꿔 보거나 응답 차이를 관찰할 공간**이 부족했습니다. 새 `PracticeWorkbench`는 각 문제의 실제 증거 문구를 사용하되 브라우저의 로컬 상태만 바꾸므로, 외부 시스템에 요청을 보내거나 코드를 실행하지 않습니다.

## 50개 노드 실습 유형 매핑

| 노드 범위 | 워크스페이스 | 사용자가 직접 조작하는 값 | 관찰 목표 |
|---|---|---|---|
| 01–10 | Source & Storage Inspector | 화면 요소·브라우저 저장 값 | 클라이언트 값과 서버 신뢰 경계 구분 |
| 11–20 | Local Request Replay | GET/POST 메서드·요청 값 | 요청 방식과 서버 검증 책임 구분 |
| 21–30 | Safe Output Sandbox | 테스트 문자열 | 원본 값과 안전한 텍스트 출력 비교 |
| 31–40 | Authorization Gate | 리소스 식별자·요청 메서드 | 식별자와 권한의 차이 관찰 |
| 41–50 | Evidence Noteboard | 사실 기반 분석 메모 | 관찰·가설·방어 권고 분리 |

## 검증 기준과 단계별 적용

현재 단계에서는 모든 노드가 같은 조작형 컴포넌트를 공유하면서도 해당 문제의 증거를 응답에 반영합니다. 이후 단계에서는 문제별 응답 규칙, 업로드 메타데이터 미리보기, 세션 만료 시간선, API 필드 최소화 비교를 각 노드에 맞게 더 세분화합니다. 어느 단계에서도 객관식 정답이나 브라우저 노출 플래그를 추가하지 않으며, 플래그 검증은 기존 서버 경계를 유지합니다.

| 검증 기준 | 현재 적용 | 다음 세분화 단계 |
|---|---|---|
| 외부 네트워크 요청 없음 | `fetch` 없이 로컬 상태만 변경 | 브라우저 네트워크 로그로 재확인 |
| 코드 실행 없음 | 텍스트 전용 출력 | 위험 문자열 회귀 테스트 추가 |
| 플래그 비노출 | 워크스페이스에 정답 데이터 없음 | 번들 검사 유지 |
| 사용자 조작 가능 | 입력·메서드·실행·초기화 제공 | 문제별 상태 변화와 기록 추가 |
