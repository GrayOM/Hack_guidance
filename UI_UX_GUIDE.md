# Hack Guidance UI/UX Design Guide

## 1. 제품 경험의 정의

핵가이던스는 보안 지식이 없는 학습자가 웹 취약점을 관찰하고, 요청과 응답의 관계를 분석하며, 방어 원칙까지 이해하도록 돕는 학습 플랫폼이다. 시각 언어는 해킹 영화나 게임이 아니라 **정밀한 보안 분석 콘솔**에 가깝게 유지한다.

> 사용자가 받아야 하는 인상은 “내가 게임을 하고 있다”가 아니라 “안전한 실습 환경에서 웹의 신호를 분석하고 있다”이다.

## 2. 경험 원칙

| 원칙 | UI/UX 적용 |
|---|---|
| 관찰 우선 | 문제마다 학습 목표, 관찰 포인트, 요청·응답 정보를 먼저 제공한다. |
| 점진적 공개 | 힌트는 1~3단계로 공개하며 정답을 바로 노출하지 않는다. |
| 실무 도구의 질서 | 상태값, 타임스탬프, 고정폭 데이터, 패널 구조를 활용한다. |
| 비게임성 | 캐릭터, 세계관, 코인, 콤보, 과도한 성공 연출을 사용하지 않는다. |
| 방어까지 완결 | 완료 시 취약점 원인뿐 아니라 안전한 설계 원칙을 확인한다. |

## 3. 시각 시스템

| 항목 | 기준 |
|---|---|
| 기본 배경 | Deep Ink `#091014`와 Graphite 계열의 층위 |
| 시그니처 색 | Signal Teal `#43E6C1`, 연결·통과·확인에만 사용 |
| 상태 색 | Teal=정상/완료, Sky=학습 정보, Amber=주의/힌트, Red=차단/오류 |
| 본문 폰트 | IBM Plex Sans KR |
| 데이터 폰트 | IBM Plex Mono |
| 모서리 | 8~12px의 절제된 둥근 모서리, 과도한 pill UI 금지 |
| 질감 | 낮은 대비의 격자, 점형 데이터 맵, 약한 그레인만 사용 |

## 4. 화면 구조

### 학습 대시보드

고정 좌측 학습 경로, 중앙 문제 목록, 우측 분석 작업면의 비대칭 3열 구조를 기본으로 한다. 작은 화면에서는 좌측 학습 경로를 드로어로 이동하고, 분석 패널은 목록 아래로 이동한다.

### 문제 상세

문제 상세에는 아래 순서를 유지한다.

1. 문제 번호, 난이도, 예상 시간
2. 학습 목표
3. 관찰 포인트
4. 단계형 힌트
5. 읽기 전용 요청·응답 분석 보기
6. 완료 기록과 방어 노트

### 수료 경험

수료는 50개 모듈의 완료, 5개 레벨 평가, 5개 방어 노트의 충족을 기준으로 한다. 수료증은 공인 자격이 아니라 **Hack Guidance 과정 수료 사실**을 나타내는 확인서로 표기하고, 인증번호와 검증 URL을 제공한다.

## 5. Level 1~50 콘텐츠 설계

| 레벨 | 범위 | 주제 | 학습자 변화 |
|---|---:|---|---|
| Level 1 | 1~10 | 웹 관찰 | 소스, 파라미터, 쿠키, 헤더를 구분한다. |
| Level 2 | 11~20 | 요청과 인증 | GET/POST, 세션, 접근제어, 파일 응답을 읽는다. |
| Level 3 | 21~30 | 입력과 출력 | 입력이 브라우저·DB·파일로 흐르는 경로를 이해한다. |
| Level 4 | 31~40 | 권한과 API | 사용자 리소스, 토큰, API 응답의 경계를 분석한다. |
| Level 5 | 41~50 | 종합 분석 | 여러 단서를 근거 있는 분석 보고서로 연결한다. |

각 레벨은 8개의 학습 모듈과 2개의 평가 모듈로 구성한다. 현재 저장소의 1~5번 문제는 Level 1의 인증 검증, 파일 응답, 쿠키 권한, 출력 문맥, 입력 검증에 매핑하여 콘텐츠 의미를 유지한다.

## 6. 인터랙션 기준

모든 클릭 가능한 요소는 실제 동작을 연결한다. 힌트는 다음 단계의 관찰 기준을 열고, 문제를 선택하면 분석 작업면이 갱신되며, 완료 기록은 전체·레벨별 진행률을 즉시 반영한다. 애니메이션은 160~220ms 이내로 제한하며 `prefers-reduced-motion` 환경에서는 필수 피드백만 유지한다.

## 7. 문구 원칙

문구는 자극적인 해킹 서사보다 관찰과 분석을 말한다.

| 권장 | 지양 |
|---|---|
| “응답 구조 확인하기” | “시스템을 침투하라” |
| “서버가 신뢰하는 값을 찾으세요.” | “관리자 권한을 탈취하세요.” |
| “방어 기준을 기록하세요.” | “보상을 획득했습니다.” |

## 8. 구현 범위

현재 프런트엔드는 학습 대시보드, 50개 레벨 경로, 기존 1~5번 문제의 의미 보존, 단계형 힌트, 요청·응답 분석 패널, 진행률, 수료 기준 UI를 구현한다. 실제 문제 실행 환경, 사용자 계정 기반 영속화, 수료증 PDF 생성과 인증번호 검증은 다음 단계의 백엔드·데이터 기능으로 확장한다.

