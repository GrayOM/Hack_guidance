# Hack Guidance 보안 운영 노트

이 문서는 Hack Guidance의 **플래그 제출, 풀이 기록, 공개 랭킹 및 수료 기록**에 적용한 서버 경계를 정리한 운영용 문서입니다. 실제 플래그·인증 비밀·접속 정보는 이 문서와 저장소에 기록하지 않습니다.

## 적용된 통제

| 대상 | 서버 측 통제 | 기대 효과 |
| --- | --- | --- |
| 플래그 | `LEARNING_FLAG_MAP` 환경 변수에서만 50개 무작위 플래그를 읽고, 클라이언트 번들·공개 API·Git 추적 파일에는 저장하지 않는다. | 브라우저 개발자 도구나 저장소로 실제 정답을 조회할 수 없다. |
| 제출 검증 | 인증된 제출 API만 허용하며, 문제 ID 범위·입력 형식을 검증하고 정상화한 입력을 서버 비밀 맵과 비교한다. | 임의 노드·비정상 입력으로 풀이 상태를 만들 수 없다. |
| 대입 공격 | 사용자별 분당 12회 제출 제한을 적용한다. | 짧은 시간 내 자동 대입 시도를 제한한다. |
| 풀이 기록 | 사용자 ID와 문제 ID의 고유 조합을 저장하고, 이미 해결한 문제는 최초 해결 시각을 보존한다. | 중복 제출로 해결 시각이나 랭킹 기준을 조작할 수 없다. |
| 공개 랭킹 | 서버가 인증된 풀이 기록만 집계하고, 해결 수 내림차순·마지막 해결 시각 오름차순으로 정렬한다. | 클라이언트가 점수·순위를 직접 지정할 수 없다. |
| 수료 기록 | 서버가 해결 수 50개를 확인한 뒤에만 발급하며, 기존 발급 기록이 있으면 동일 기록을 반환한다. | 미완료 사용자의 발급 및 중복 발급을 방지한다. |
| 검증 코드 | 수료 기록 코드는 UUID 기반 난수로 생성한다. | 순번·시간 기반 코드의 예측 가능성을 줄인다. |

## 운영 권고

`LEARNING_FLAG_MAP`은 배포 환경의 비밀 설정으로만 관리해야 합니다. 이 값이 로그, 이슈, 화면 캡처, 저장소 또는 외부 문서에 노출되었다고 판단되면 즉시 새 50개 맵을 생성해 교체하고, 노출된 플래그는 폐기해야 합니다. 플래그를 회전해도 이미 서버에 저장된 해결 기록은 유지되지만, 아직 제출하지 않은 사용자는 새 플래그 기준으로 풀이해야 합니다.

Git 이력에 과거 플래그가 존재했다면 현재 코드를 수정하는 것만으로는 충분하지 않습니다. 이력을 정리해 원격 기본 브랜치를 강제 갱신하고, 기존 값은 이미 폐기된 상태로 유지해야 합니다. 이미 복제되었거나 캐시된 외부 사본까지 회수할 수는 없으므로, **비밀 회전**을 이력 정리와 함께 수행해야 합니다.

운영 환경에서는 환경 변수 접근 권한을 배포 관리자에게만 제한하고, 서버 로그에 `LEARNING_FLAG_MAP` 또는 원문 플래그를 출력하지 않아야 합니다. 수료 코드·공개 검증 API도 실패 메시지에서 사용자 식별 정보나 내부 상태를 과도하게 드러내지 않도록 유지합니다.

## 회귀 검증 기준

`pnpm check && pnpm test && pnpm build`는 타입 검사, 실제 비밀 맵의 50개 항목 검증, 인증된 제출 API 경로, 제출 제한, 랭킹 정렬, 수료 조건 및 난수 수료 코드 형식을 포함한 회귀 검증 기준입니다. 비밀값 교체나 제출 API 변경 후에는 반드시 이 명령을 다시 실행합니다.
